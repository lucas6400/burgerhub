{
  "name": "burgerhub",
  "private": true,
  "version": "0.1.0",
  "description": "SaaS multi-tenant de atendimento e gestão para hamburguerias — WhatsApp, Cardápio Digital, KDS, Estoque e Financeiro",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:api dev:web",
    "dev:api": "npm run dev --workspace=@burgerhub/api",
    "dev:web": "npm run dev --workspace=@burgerhub/web",
    "build": "npm run build --workspace=@burgerhub/api && npm run build --workspace=@burgerhub/web",
    "db:push": "npm run db:push --workspace=@burgerhub/api",
    "db:seed": "npm run db:seed --workspace=@burgerhub/api"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
